# 2026-04-17 — Daily Memory

## SurvivorPulse Back-Testing Prototype — Status

**Repo:** `~/Projects/SurvivorPulse-BackTesting-Prototype` (renamed from CMEA-Prototype)
**Git:** `git@github.com:mwolff328-stack/SurvivorPulse-BackTesting-Prototype.git`

### Completed (cumulative)
- Full back-tester prototype: React client-side app simulating NFL survivor pool strategies against 2021-2025 historical data
- Dark-mode design system (Linear-inspired), all panels, week selector, SWR cache
- Strategy system: 6 templates, configurable weights (wp/ps/fv auto-sum 100), lookahead, decay, min win prob, EV mode
- Per-entry strategy assignment, 1-50 entries, buyback toggle
- "Find Best Strategy" optimization: 27+ strategies, per-entry optimization (15 mixed scenarios)
- Advanced modes: Coordinated Diversification, Adaptive Blend, Scarcity-Aware
- Expert game context filters: Avoid Divisional, Prefer Home (soft/hard)
- Performance Summary panel, Available Teams table (sortable, sticky header, frozen Team column)
- Progressive week loading (batch of 4)
- Access gate (code: SURVIVOR2026) + name/email capture
- Feedback system: 5 progressive questions, persistent mode, localStorage + Notion proxy
- Admin page at /admin for feedback export
- User guide drawer, tooltips on all fields
- 17-step interactive tutorial overlay with direct element highlighting
- 285 tests passing across 12 test files
- Data loaded into production Neon DB for all 5 seasons

### In Progress / Open Issues
- **Tutorial Step 7 ("Edit Parameters") highlight timing**: The `data-tutorial-step="strategy-params"` div only renders when `paramsExpanded=true`. Tutorial action clicks the "Edit params" button but element may not exist for highlighting after React re-render. Pushed delay increase (300ms + 600ms retry) but may still need further work.
- **Scroll positioning for tutorial steps**: For each step (except Step 16: Help), the page should scroll so the Explanation Modal appears above the highlighted section. Not yet implemented.

### Key Files
- `TutorialOverlay.tsx` — tutorial step definitions, actions, overlay rendering
- `PoolConfigPanel.tsx` — contains strategy-params section (conditional render)
- `src/lib/assignIndependentPicks.ts` — core simulation logic
- `src/lib/optimization.ts` — optimization grid and scenarios

### Next Steps
1. Fix tutorial Step 7 highlighting (timing issue with conditional render)
2. Implement scroll positioning for tutorial modal above highlighted sections
3. Deploy to production and send to TAM users
4. Monitor feedback in Notion "Backtester Feedback" database
5. Iterate based on user feedback

### Operational Notes
- Claude Code rate limit resets at 4am PDT (or 11am PDT depending on tier)
- Notion feedback DB: `82ca7841-8bb8-4796-a5ab-f8f1f792ec5e`
- Notion API key must be set as `NOTION_API_KEY` Replit secret on SurvivorPulse deployment
- Production URL: `survivorpulse-backtester-prototype.replit.app`
# 2026-04-17 — Daily Memory

## SurvivorPulse Back-Testing Prototype — Status

**Repo:** `~/Projects/SurvivorPulse-BackTesting-Prototype` (renamed from CMEA-Prototype)
**Git:** `git@github.com:mwolff328-stack/SurvivorPulse-BackTesting-Prototype.git`

### Completed (cumulative)
- Full back-tester prototype: React client-side app simulating NFL survivor pool strategies against 2021-2025 historical data
- Dark-mode design system (Linear-inspired), all panels, week selector, SWR cache
- Strategy system: 6 templates, configurable weights (wp/ps/fv auto-sum 100), lookahead, decay, min win prob, EV mode
- Per-entry strategy assignment, 1-50 entries, buyback toggle
- "Find Best Strategy" optimization: 27+ strategies, per-entry optimization (15 mixed scenarios)
- Advanced modes: Coordinated Diversification, Adaptive Blend, Scarcity-Aware
- Expert game context filters: Avoid Divisional, Prefer Home (soft/hard)
- Performance Summary panel, Available Teams table (sortable, sticky header, frozen Team column)
- Progressive week loading (batch of 4)
- Access gate (code: SURVIVOR2026) + name/email capture
- Feedback system: 5 progressive questions, persistent mode, localStorage + Notion proxy
- Admin page at /admin for feedback export
- User guide drawer, tooltips on all fields
- 17-step interactive tutorial overlay with direct element highlighting
- 285 tests passing across 12 test files
- Data loaded into production Neon DB for all 5 seasons

### In Progress / Open Issues
- **Tutorial Step 7 ("Edit Parameters") highlight timing**: The `data-tutorial-step="strategy-params"` div only renders when `paramsExpanded=true`. Tutorial action clicks the "Edit params" button but element may not exist for highlighting after React re-render. Pushed delay increase (300ms + 600ms retry) but may still need further work.
- **Scroll positioning for tutorial steps**: For each step (except Step 16: Help), the page should scroll so the Explanation Modal appears above the highlighted section. Not yet implemented.

### Key Files
- `TutorialOverlay.tsx` — tutorial step definitions, actions, overlay rendering
- `PoolConfigPanel.tsx` — contains strategy-params section (conditional render)
- `src/lib/assignIndependentPicks.ts` — core simulation logic
- `src/lib/optimization.ts` — optimization grid and scenarios

### Next Steps
1. Fix tutorial Step 7 highlighting (timing issue with conditional render)
2. Implement scroll positioning for tutorial modal above highlighted sections
3. Deploy to production and send to TAM users
4. Monitor feedback in Notion "Backtester Feedback" database
5. Iterate based on user feedback

### Operational Notes
- Claude Code rate limit resets at 4am PDT (or 11am PDT depending on tier)
- Notion feedback DB: `82ca7841-8bb8-4796-a5ab-f8f1f792ec5e`
- Notion API key must be set as `NOTION_API_KEY` Replit secret on SurvivorPulse deployment
- Production URL: `survivorpulse-backtester-prototype.replit.app`

---

## #infra Session — 2026-04-17

### Claude Code ↔ OpenClaw Integration (3 options wired)

**Option 1: MCP (Claude Code calls Luigi's tools)**
- `openclaw mcp serve` exposes OpenClaw as MCP server
- Config written to `~/.claude/settings.json` with `openclaw` as stdio MCP server
- Claude Code auto-connects on startup; use `/mcp` inside Claude Code to verify

**Option 2: System event (Claude Code notifies Luigi)**
- Claude Code runs: `openclaw system event --text "Done: ..." --mode now`
- Fires immediate wake event into Luigi's session
- Already worked before; no setup needed

**Option 3: ACP (interactive Claude Code session from Discord)**
- Already wired from 2026-04-11 session
- Spawn with `/acp spawn claude --thread auto` or `--bind here`
- Issue discovered: `/acp spawn claude --thread auto` fails with "Session binding adapter failed to bind target conversation" in #dev-sessions channel
- Likely cause: bot missing **Create Public Threads** permission in that channel, OR need to use `--bind here` instead
- Next step: try `/acp spawn claude --bind here` in #infra or check bot permissions in #dev-sessions

### When to use each
- Option 2: Claude Code notifies Luigi when task finishes
- Option 3: Mike talks directly to Claude Code from Discord
- Option 1: Claude Code reads/writes Luigi's memory or triggers tools mid-task

### ACP troubleshooting status
- `/acp` slash command IS registered in Discord (autocomplete shows up)
- Gateway receives interaction events
- `--thread auto` fails; `--bind here` not yet tested
- Action: Mike to try `--bind here` or check bot thread permissions
